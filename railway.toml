[build]
builder = "nixpacks"
buildCommand = "conda env create -f environment.yml"

[deploy]
startCommand = "conda run -n myproject uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10


